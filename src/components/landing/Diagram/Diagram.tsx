import styles from "./Diagram.module.css";

export default function Diagram() {
  return (
    <section id="features" className={styles.pricing}>
      <div className={`${styles.content}`}>
        <h2>Stuff</h2>
      </div>
    </section>
  );
}
